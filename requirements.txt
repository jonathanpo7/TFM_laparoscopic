# GPU con NVIDIA/CUDA: instalar torch ANTES de este archivo, con el índice
# de PyTorch que corresponda a tu versión de CUDA, ej. (CUDA 12.1):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# Sin GPU NVIDIA (solo CPU, ej. Intel Core Ultra): no hace falta nada especial,
# `pip install -r requirements.txt` ya instala la build CPU desde PyPI.
torch==2.5.1
ultralytics==8.4.78
boxmot==22.0.0
opencv-python==4.13.0
numpy>=1.24,<2.3
scikit-learn==1.7.2
